import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'google-ads-preview',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
